The emails-fork exploration I dispatched has now returned. It **corroborates every code-level claim in the review above** (getter bodies and settings keys at `emails.ex:1286/2145/2171/2189`; `migrate_legacy/0` confirmed as a no-op default; settings LiveView at `web/settings.ex` + `web/settings.html.heex`). Two refinements worth adding to the record:

**Sharpens §2 (B3/B4 scoping) — the plan's "creds only" line is correctly drawn.** The emails settings LV (`settings.ex:585-634`) writes a *batch*: `aws_access_key_id`, `aws_secret_access_key`, `aws_region` **plus** `aws_ses_configuration_set` (default `"phoenixkit-tracking"`), `aws_sns_topic_arn`, `aws_sqs_queue_url/arn/dlq_url`, `sqs_polling_interval_ms`. The plan correctly moves only the three credential keys into Integrations and leaves the queue/topic/config-set settings in place — which is exactly the user's binding split (Integrations = keys; everything else = SendProfile). So B4's `migrate_legacy` is right to touch only `access_key_id`/`secret_access_key`/`aws_region`. ✓

**One new integration gap for the SendProfile `advanced` jsonb.** The `advanced` column is specified (D1) to carry "SES queue/config-set" per-profile settings — but `aws_ses_configuration_set` is owned and read by the *emails* module (it flows into SES sending/tracking via the emails Provider). For a newsletters SendProfile's per-profile config-set to actually take effect, `deliver_via_integration/3` (C3) would have to pass it as a Swoosh provider option on the per-call SES config; otherwise the emails module's *global* `aws_ses_configuration_set` wins and `advanced` is inert for SES. The plan doesn't connect these. Minor for a "foundation" phase (a single global config-set is fine for now), but flag it so Phase-4 rotation/per-account-config-set doesn't rediscover it.

**One consistency win for `provider_kind` (§5).** The emails package *already* maps `Swoosh.Adapters.AmazonSES → "aws_ses"` and `Swoosh.Adapters.SMTP → "smtp"` as provider-detection names (`utils.ex:36-40`, mirrored in `interceptor.ex:718-722`). So the plan's `provider_kind` enum `~w(aws_ses smtp brevo_api brevo_smtp)` reuses two existing names — good. Note `brevo_api`/`brevo_smtp` are *not* in that detection map, so newsletters-via-Brevo sends won't be classified by the emails interceptor; that's expected (Brevo bounce/complaint tracking is Phase 8) but worth stating so no one expects open/click tracking to flow through the emails SES path for Brevo.

**Verdict unchanged: Sound-with-changes.** The central blocking item remains the credential-detection mismatch (§1) for `aws_ses`/`smtp`/`brevo_smtp` — fix that, tighten the B1/C1 tests to assert `get_credentials/1` round-trips the secret, correct the `add_connection` match in B4, and the Integrations path becomes real instead of masked by the legacy fallback.
